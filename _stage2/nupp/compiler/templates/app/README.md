# ${name}

A Nupp project.

```sh
nupp check       # type-check the sources
nupp build       # compile into build/
nupp test        # build, then run tests/run.lua
nupp task start  # run the program
```
