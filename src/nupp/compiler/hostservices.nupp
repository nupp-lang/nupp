module nupp.compiler.hostservices

-- Native compiler implementation services. Importing this module is the explicit
-- boundary that admits filesystem/process/C-header support into a compiler host.

export = {
    cheader = require("nupp.compiler.cinterop.cheader"),
    comptimeworker = require("nupp.compiler.comptime.worker"),
    fs = require("nupp.compiler.fs"),
    hash = require("nupp.compiler.nativehash"),
}
