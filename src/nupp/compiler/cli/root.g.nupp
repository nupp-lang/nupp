module nupp.compiler.cli.root

local cliModule = require("nupp.cli")

--- Nupp compiler and project tool.
---
--- Examples:
---
---     nupp check
---     nupp build
---     nupp run src/main.nupp
@cli(name = "nupp", group = true)
@derive(cliModule.Command)
export record Root
end

--- Show general or command-specific help.
---
--- With no command, prints the command list.
---
--- Examples:
---
---     nupp help
---     nupp help build
---     nupp help lsp rename
@cli("help")
@derive(cliModule.Command)
export record Help
    --- Command path to describe.
    @cli(remainder = true, value = "COMMAND")
    command: {string} = {}

    function run(self): integer
        return 0
    end
end

--- Returns Nupp's top-level commands in help order.
function Root.subcommands(): {cliModule.CommandType}
    return {
        require("nupp.compiler.cli.initcommand").commandType,
        require("nupp.compiler.cli.ast").commandType,
        require("nupp.compiler.cli.aot").commandType,
        require("nupp.compiler.cli.bc").commandType,
        require("nupp.compiler.cli.check").commandType,
        require("nupp.compiler.cli.fmt").commandType,
        require("nupp.compiler.cli.build").commandType,
        require("nupp.compiler.cli.clean").commandType,
        require("nupp.compiler.cli.bench").commandType,
        require("nupp.compiler.cli.lints").commandType,
        require("nupp.compiler.cli.ownershipaudit").commandType,
        require("nupp.compiler.cli.explain").commandType,
        require("nupp.compiler.cli.reference").commandType,
        require("nupp.compiler.cli.completionscommand").commandType,
        require("nupp.compiler.cli.test").commandType,
        require("nupp.compiler.cli.task").commandType,
        require("nupp.compiler.cli.doc").commandType,
        require("nupp.compiler.cli.fixpoint").commandType,
        require("nupp.compiler.cli.run").commandType,
        require("nupp.compiler.cli.importc").commandType,
        require("nupp.compiler.cli.migrate").commandType,
        require("nupp.compiler.cli.exportc").commandType,
        require("nupp.compiler.cli.rock").commandType,
        require("nupp.compiler.cli.lsp").commandType,
        require("nupp.compiler.cli.version").commandType,
        Help,
    }
end
