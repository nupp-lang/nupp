module nupp.io.uri.provider

const contracts = require("nupp.runtime.services.contracts")

export = contracts.uri:assemble(function(): contracts.UriTextProvider
    local native = contracts.uri:lookup("nupp.native")
    if native ~= nil then
        return native
    end

    return contracts.uri:require("nupp.browser")
end)
