module nupp.io.path.provider

const contracts = require("nupp.runtime.services.contracts")

export = contracts.path:assemble(function(): contracts.PathProvider
    local native = contracts.path:lookup("nupp.native")
    if native ~= nil then
        return native
    end

    return contracts.path:require("nupp.browser")
end)
