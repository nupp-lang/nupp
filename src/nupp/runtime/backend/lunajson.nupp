module nupp.runtime.backend.lunajson

--[[
The portable JSON backend. The declaration names the checked adapter while the
target's dependency declaration owns the pinned third-party implementation.
]]

export = {name = "lunajson", seams = {["data.json"] = "nupp.runtime.provider.lunajson",},}
