@!internal
module nupp.runtime.seam.browserstoragesuite

local suite = {}

function suite.test(provider: any): (boolean, string?)
    local problem = require("nupp.runtime.seam.browserstorage").validate(provider)
    return problem == nil, problem
end

export = suite
