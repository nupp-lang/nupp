@!internal

module nupp.runtime.provider

--[[
Built-in runtime implementations, imported explicitly by their consuming facade.

Third-party implementations target the library's public `.spi` interfaces and
shared types. Each implementation owns the resources described by its interface
and initializes independently in every Lua state.
]]
