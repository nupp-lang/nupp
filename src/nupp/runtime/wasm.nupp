@!internal
module nupp.runtime.wasm

local representation = require("nupp.runtime.representation")
export = assert(representation.host, "nupp.runtime.wasm: the target memory representation has no host implementation")
