module tests.fixtures.cliconstantinvalid

local cli = require("nupp.cli")

@derive(cli.Arguments)
local record InvalidConstant
    @cli(arity = "optionalAttached", constant = true)
    profile: string?
end
