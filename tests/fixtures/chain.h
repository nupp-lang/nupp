/* Test fixture for nupp import-c: a header whose vocabulary is a chain built
   in an included file, the shape every real system header has. */

#include "chain_base.h"

chain_size_t chain_len(const char *s);
