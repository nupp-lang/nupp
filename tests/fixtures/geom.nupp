-- Test fixture: a typed module.
local function make(x: number, y: number): {x: number, y: number}
    return {x = x, y = y}
end

local function dist2(p: {x: number, y: number}): number
    return p.x * p.x + p.y * p.y
end

return {make = make, dist2 = dist2, version = 1}
