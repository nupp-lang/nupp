module codec

local codec = {}

function codec.digest(input: string): number
    local value = 0
    for index = 1, #input do
        value = value + string.byte(input, index) * index
    end

    return value
end

export = codec
