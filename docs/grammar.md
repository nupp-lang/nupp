# Grammar

Nupp's syntax is defined by a normative ABNF grammar. It is embedded below
straight from [`docs/grammar.abnf`](../grammar.abnf) rather than retyped here,
so this page can never drift from the grammar the parser is actually checked
against.

<<< @docs/grammar.abnf
