-- Browser providers keep the Nupp API in checked source. Their work crosses
-- one suspension boundary to the application Worker and its browser APIs.

local random = require("nupp.random")
local time = require("nupp.time")
local mac = require("nupp.mac")
local digests = require("nupp.digest")
local uuid = require("nupp.util")

time.sleep(10)
local key = uuid.uuid4()
local digest = digests.hexDigest("sha256", random.randomBytes(32))
print(key, digest)
print(mac.hexDigest("hmac-sha256", "key", "browser message"))

-- try breaking it: pass a negative count to randomBytes and read the checked
-- provider error
