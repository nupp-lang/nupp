/* The same differential as `checks/mixedwidth.c`, over the companion kernel
 * that isolates one half of the mixed-width cost. The bodies differ; what is
 * asked of them does not, so the driver is shared rather than copied.
 */
#include "mixedwidth.c"
