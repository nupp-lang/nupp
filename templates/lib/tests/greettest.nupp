local test = require("nupp.test")
local library = require("${moduleName}")
local suite = {}

function suite.greetsByName(): nil
    test.equal(library.greet("Nupp"), "Hello, Nupp!")
end

return suite
