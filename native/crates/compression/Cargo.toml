[package]
name = "nupp-native-compression"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
flate2 = { version = "1", default-features = false, features = ["zlib-rs"] }
