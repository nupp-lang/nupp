[package]
name = "nupp-native-runtime"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
nupp-native-abi = { path = "../abi" }
